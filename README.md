# Python Password Generator (OOP Version)

## 🔐 Strong Password Generator with Classes

This project is a **Python script** that generates strong, random passwords using **Object-Oriented Programming (OOP)**.  
It allows the user to choose between **simple password** or **custom password** modes.  

---

## ⚙️ Features

- User can input desired password length  
- Two modes: simple or custom  
  - Simple mode: computer generates the password automatically  
  - Custom mode: user can choose which character types to include (lowercase, uppercase, digits, symbols)  
- Randomly selects characters according to the chosen mode  
- Final password is printed as a string  

---

## 🛠️ How it works

1. The script asks the user for the desired password length.  
2. The user selects either **simple** or **custom** password mode.  
3. If custom mode is selected, the user chooses which character types to include.  
4. The script generates and prints the final password.  

---

## 💻 Usage

```
python main.py
# Then enter password length and choose mode (simple or custom)
```
## ⚡ Example Output
```

Enter password length: 12
Choose mode: 1 (Simple) or 2 (Custom): 2
Lower?(y/n): y
Upper?(y/n): n
Digits?(y/n): y
Symbols?(y/n): n
Generated password: ab7kq9mzd2pa
```

# تولیدکننده رمز عبور با پایتون (نسخه شی‌گرایی)

## 🔐 تولید رمز عبور قوی با استفاده از کلاس‌ها

این پروژه یک **اسکریپت پایتون** است که با استفاده از **شی‌گرایی (OOP)**، رمزهای عبور قوی و تصادفی تولید می‌کند.  
کاربر می‌تواند بین **رمز عبور ساده** یا **رمز عبور سفارشی** یکی را انتخاب کند.  

---

## ⚙️ ویژگی‌ها

- کاربر می‌تواند طول رمز عبور را وارد کند  
- دو حالت برای تولید رمز عبور: ساده یا سفارشی  
  - حالت ساده: کامپیوتر رمز عبور را به‌صورت خودکار تولید می‌کند  
  - حالت سفارشی: کاربر می‌تواند مشخص کند کدام نوع کاراکترها در رمز حضور داشته باشند (حروف کوچک، حروف بزرگ، اعداد، نمادها)  
- انتخاب تصادفی کاراکترها مطابق با حالت انتخاب‌شده  
- نمایش رمز عبور نهایی به‌صورت رشته  

---

## 🛠️ نحوه کار

1. اسکریپت طول موردنظر رمز عبور را از کاربر می‌پرسد.  
2. کاربر حالت **ساده** یا **سفارشی** را انتخاب می‌کند.  
3. اگر حالت سفارشی انتخاب شود، کاربر نوع کاراکترهای موردنظر را مشخص می‌کند.  
4. اسکریپت رمز عبور نهایی را تولید و چاپ می‌کند.  

---

## 💻 نحوه استفاده

```
python main.py
# سپس طول رمز عبور و حالت موردنظر (ساده یا سفارشی) را وارد کنید
```

## ⚡ مثال خروجی
```
Enter password length: 12
Choose mode: 1 (Simple) or 2 (Custom): 2
Lower?(y/n): y
Upper?(y/n): n
Digits?(y/n): y
Symbols?(y/n): n
Generated password: ab7kq9mzd2pa